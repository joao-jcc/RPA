[project]
name = "rpa-api"
version = "0.1.0"
description = "FastAPI service for RPA automation"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "playwright",
    "pydantic-settings",
    "python-dotenv",
    "requests",
    "uvicorn",
]

[tool.uv]
package = false
